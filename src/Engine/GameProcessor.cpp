#include "../../include/Engine/GameProcessor.h"

using namespace object_models;

GameData engine::GameProcessor::process(GameData game, ActionInfo action)
{
    return object_models::GameData();
}
